{
  "name": "ieee-ikcu-hub",
  "private": true,
  "version": "0.1.0",
  "description": "IEEE İKÇÜ Öğrenci Kolu iç operasyon portalı",
  "workspaces": [
    "apps/hub",
    "firebase"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm run dev -w apps/hub",
    "build": "npm run build -w apps/hub",
    "typecheck": "npm run typecheck -w apps/hub",
    "test": "npm run test -w apps/hub",
    "test:rules": "firebase emulators:exec --only firestore --project demo-ieee-hub \"npm run test -w firebase\"",
    "test:e2e": "firebase emulators:exec --only auth,firestore --project demo-ieee-hub \"npm run test -w apps/hub\"",
    "emulators": "firebase emulators:start --only auth,firestore --project demo-ieee-hub --import=firebase/demo-data",
    "deploy": "npm run build && firebase deploy --only hosting,firestore",
    "emulators:empty": "firebase emulators:start --only auth,firestore --project demo-ieee-hub",
    "demo:rebuild": "firebase emulators:exec --only auth,firestore --project demo-ieee-hub --export-on-exit=firebase/demo-data \"npm run seed -w apps/hub\"",
    "smoke": "firebase emulators:exec --only auth,firestore --project demo-ieee-hub --import=firebase/demo-data \"node apps/hub/scripts/smoke.mjs\""
  }
}
